import type { InternalApi } from "../../device-action/DeviceAction";
import { UnknownDAError, UnsupportedApplicationDAError, UnsupportedFirmwareDAError } from "../../device-action/os/Errors";
import type { ApplicationDependency } from "../../device-action/os/InstallOrUpdateApps/types";
import type { Application } from "../../../internal/manager-api/model/Application";
export type BuildAppsInstallPlanTaskArgs = {
    /**
     * List of applications to install or update
     */
    readonly applications: ApplicationDependency[];
    /**
     * Indicates if the device action should fail if an application is missing from the app store.
     */
    readonly allowMissingApplication: boolean;
};
export type BuildAppsInstallPlanTaskResult = {
    installPlan: Application[];
    alreadyInstalled: string[];
    missingApplications: string[];
} | {
    error: UnknownDAError | UnsupportedApplicationDAError | UnsupportedFirmwareDAError;
};
export declare class BuildAppsInstallPlanTask {
    private readonly api;
    private readonly args;
    private readonly deviceModelId;
    constructor(api: InternalApi, args: BuildAppsInstallPlanTaskArgs);
    run(): BuildAppsInstallPlanTaskResult;
    private validateConstraint;
}
//# sourceMappingURL=BuildAppsInstallPlanTask.d.ts.map