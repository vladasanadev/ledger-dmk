import { type DeviceAction, type DeviceActionIntermediateValue, type InternalApi } from "../../device-action/DeviceAction";
import { type DeviceActionState } from "../../device-action/model/DeviceActionState";
import { type DmkError } from "../../Error";
/**
 * Test that the states emitted by a device action match the expected states.
 * @param deviceAction The device action to test.
 * @param expectedStates The expected states.
 * @param internalApi
 * @param callbacks { onDone, onError } The callbacks to call when the test is done or an error occurs.
 */
export declare function testDeviceActionStates<Output, Input, Error extends DmkError, IntermediateValue extends DeviceActionIntermediateValue>(deviceAction: DeviceAction<Output, Input, Error, IntermediateValue>, expectedStates: Array<DeviceActionState<Output, Error, IntermediateValue>>, internalApi: InternalApi, { onDone, onError, }: {
    onDone: () => void;
    onError: (error: Error) => void;
}): {
    observable: import("rxjs").Observable<DeviceActionState<Output, Error, IntermediateValue>>;
    cancel: () => void;
};
//# sourceMappingURL=testDeviceActionStates.d.ts.map