import { type CommandResult } from "../../command/model/CommandResult";
import type { InternalApi } from "../../device-action/DeviceAction";
import { type CustomImage, type FirmwareUpdateContext, type FirmwareVersion } from "../../device-session/DeviceSessionState";
import { type DeviceVersion } from "../../../internal/manager-api/model/Device";
import { type FinalFirmware } from "../../../internal/manager-api/model/Firmware";
export type GetFirmwareMetadataTaskResult = CommandResult<{
    deviceVersion: DeviceVersion;
    firmware: FinalFirmware;
    firmwareVersion: FirmwareVersion;
    firmwareUpdateContext: FirmwareUpdateContext;
    customImage: CustomImage;
}>;
export declare class GetFirmwareMetadataTask {
    private readonly api;
    constructor(api: InternalApi);
    run(): Promise<GetFirmwareMetadataTaskResult>;
}
//# sourceMappingURL=GetFirmwareMetadataTask.d.ts.map