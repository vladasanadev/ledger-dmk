import { type DeviceSessionId } from "../../device-session/types";
import { LoggerPublisherService } from "../../logger-publisher/service/LoggerPublisherService";
import { type DeviceSessionService } from "../../../internal/device-session/service/DeviceSessionService";
export type DisableDeviceSessionRefresherUseCaseArgs = {
    sessionId: DeviceSessionId;
    blockerId: string;
};
/**
 * Disable the device session refresher.
 */
export declare class DisableDeviceSessionRefresherUseCase {
    private readonly _sessionService;
    private readonly _logger;
    constructor(_sessionService: DeviceSessionService, loggerFactory: (tag: string) => LoggerPublisherService);
    execute({ sessionId, blockerId }: DisableDeviceSessionRefresherUseCaseArgs): () => void;
}
//# sourceMappingURL=DisableDeviceSessionRefresher.d.ts.map