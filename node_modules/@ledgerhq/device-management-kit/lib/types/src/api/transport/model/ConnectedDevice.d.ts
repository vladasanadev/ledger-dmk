import { type DeviceId, type DeviceModelId } from "../../device/DeviceModel";
import { type ConnectionType } from "../../discovery/ConnectionType";
import { type TransportConnectedDevice } from "../../transport/model/TransportConnectedDevice";
import { type DeviceSessionId, type TransportIdentifier } from "../../types";
type ConnectedDeviceConstructorArgs = {
    readonly sessionId: DeviceSessionId;
    readonly transportConnectedDevice: TransportConnectedDevice;
};
export declare class ConnectedDevice {
    readonly id: DeviceId;
    readonly sessionId: DeviceSessionId;
    readonly modelId: DeviceModelId;
    readonly name: string;
    readonly type: ConnectionType;
    readonly transport: TransportIdentifier;
    constructor({ transportConnectedDevice: { id, deviceModel: { id: deviceModelId, productName }, type, transport, name, }, sessionId, }: ConnectedDeviceConstructorArgs);
}
export {};
//# sourceMappingURL=ConnectedDevice.d.ts.map