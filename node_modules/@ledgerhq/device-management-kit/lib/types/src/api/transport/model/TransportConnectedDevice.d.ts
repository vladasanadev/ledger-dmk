import { type DeviceId } from "../../device/DeviceModel";
import { type TransportDeviceModel } from "../../device-model/model/DeviceModel";
import { type ConnectionType } from "../../discovery/ConnectionType";
import { type SendApduFnType } from "../../transport/model/DeviceConnection";
import { type TransportIdentifier } from "../../transport/model/TransportIdentifier";
/**
 * The internal connected device.
 */
export type ConnectedDeviceConstructorArgs = {
    id: DeviceId;
    deviceModel: TransportDeviceModel;
    type: ConnectionType;
    transport: TransportIdentifier;
    sendApdu: SendApduFnType;
    name?: string;
};
export declare class TransportConnectedDevice {
    readonly id: DeviceId;
    readonly deviceModel: TransportDeviceModel;
    readonly sendApdu: SendApduFnType;
    readonly type: ConnectionType;
    readonly transport: TransportIdentifier;
    readonly name?: string;
    constructor({ id, deviceModel, type, transport, sendApdu, name, }: ConnectedDeviceConstructorArgs);
}
//# sourceMappingURL=TransportConnectedDevice.d.ts.map