import { LogLevel } from "../../logger-subscriber/model/LogLevel";
import { type LogSubscriberOptions } from "../../logger-subscriber/model/LogSubscriberOptions";
import { type LoggerSubscriberService } from "../../logger-subscriber/service/LoggerSubscriberService";
export declare class ConsoleLogger implements LoggerSubscriberService {
    private readonly maxLevel;
    constructor(level?: LogLevel);
    log(level: LogLevel | null, message: string, options: LogSubscriberOptions): void;
    private logWithData;
}
//# sourceMappingURL=ConsoleLogger.d.ts.map