import { LogLevel } from "../../logger-subscriber/model/LogLevel";
import { type LogSubscriberOptions } from "../../types";
import { type LoggerSubscriberService } from "./LoggerSubscriberService";
/**
 * This function is used to format the logs to JSON format,
 * remove circular dependencies and do some extra formatting.
 * */
export declare function getJSONStringifyReplacer(): (key: string, value: unknown) => unknown;
export declare class WebLogsExporterLogger implements LoggerSubscriberService {
    private logs;
    log(level: LogLevel, message: string, options: LogSubscriberOptions): void;
    private formatLogsToJSON;
    /**
     * Export logs to JSON file.
     */
    exportLogsToJSON(): void;
}
//# sourceMappingURL=WebLogsExporterLogger.d.ts.map