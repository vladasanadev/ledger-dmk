import { Command } from "../../command/Command";
import { CommandResult } from "../../command/model/CommandResult";
import { type LoggerPublisherService } from "../../logger-publisher/service/LoggerPublisherService";
import type { DeviceSessionService } from "../../../internal/device-session/service/DeviceSessionService";
export type SendCommandUseCaseArgs<Response, ErrorStatusCodes, Args = void> = {
    /**
     * The device session id.
     */
    readonly sessionId: string;
    /**
     * The command to send.
     */
    readonly command: Command<Response, ErrorStatusCodes, Args>;
    /**
     * The timeout to abort the command.
     */
    readonly abortTimeout?: number;
};
/**
 * Sends a command to a device through a device session.
 */
export declare class SendCommandUseCase {
    private readonly _sessionService;
    private readonly _logger;
    constructor(sessionService: DeviceSessionService, loggerFactory: (tag: string) => LoggerPublisherService);
    /**
     * Sends a command to a device through a device session.
     *
     * @param sessionId - The device session id.
     * @param command - The command to send.
     * @returns The response from the command.
     */
    execute<Response, Args, ErrorStatusCodes>({ sessionId, command, abortTimeout, }: SendCommandUseCaseArgs<Response, Args, ErrorStatusCodes>): Promise<CommandResult<Response, ErrorStatusCodes>>;
}
//# sourceMappingURL=SendCommandUseCase.d.ts.map