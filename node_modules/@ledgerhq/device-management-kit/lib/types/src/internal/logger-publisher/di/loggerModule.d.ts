import { ContainerModule } from "inversify";
import { type LoggerSubscriberService } from "../../../api/logger-subscriber/service/LoggerSubscriberService";
type FactoryProps = {
    subscribers: LoggerSubscriberService[];
};
export declare const loggerModuleFactory: ({ subscribers }?: FactoryProps) => ContainerModule;
export {};
//# sourceMappingURL=loggerModule.d.ts.map