import { LoggerPublisherService } from "../../../api/logger-publisher/service/LoggerPublisherService";
import type { DeviceSessionId } from "../../../api/types";
import type { DeviceSessionService } from "../../device-session/service/DeviceSessionService";
import { type TransportService } from "../../transport/service/TransportService";
/**
 * The arguments for the DisconnectUseCase.
 */
export type DisconnectUseCaseArgs = {
    /**
     * Device session identifier from `DeviceManagementKit.connect`.
     */
    sessionId: DeviceSessionId;
};
/**
 * Disconnects to a discovered device.
 */
export declare class DisconnectUseCase {
    private readonly _transportService;
    private readonly _sessionService;
    private readonly _logger;
    constructor(transportService: TransportService, sessionService: DeviceSessionService, loggerFactory: (tag: string) => LoggerPublisherService);
    execute({ sessionId }: DisconnectUseCaseArgs): Promise<void>;
}
//# sourceMappingURL=DisconnectUseCase.d.ts.map