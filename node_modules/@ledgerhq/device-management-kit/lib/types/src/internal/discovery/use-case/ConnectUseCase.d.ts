import { DeviceSessionId } from "../../../api/device-session/types";
import { LoggerPublisherService } from "../../../api/logger-publisher/service/LoggerPublisherService";
import { ConnectedDevice } from "../../../api/transport/model/ConnectedDevice";
import { DiscoveredDevice } from "../../../api/transport/model/DiscoveredDevice";
import { DeviceSessionRefresherOptions } from "../../device-session/model/DeviceSession";
import type { DeviceSessionService } from "../../device-session/service/DeviceSessionService";
import type { ManagerApiService } from "../../manager-api/service/ManagerApiService";
import type { SecureChannelService } from "../../secure-channel/service/SecureChannelService";
import { type TransportService } from "../../transport/service/TransportService";
/**
 * The arguments for the ConnectUseCase.
 */
export type ConnectUseCaseArgs = {
    /**
     * Discovered device or connected device.
     */
    device: DiscoveredDevice | ConnectedDevice;
    /**
     * sessionRefresherOptions - optional
     * isRefresherDisabled - whether the refresher is disabled
     * pollingInterval - optional - the refresh interval in milliseconds
     */
    sessionRefresherOptions?: DeviceSessionRefresherOptions;
};
/**
 * Connects to a discovered device.
 */
export declare class ConnectUseCase {
    private readonly _transportService;
    private readonly _sessionService;
    private readonly _loggerFactory;
    private readonly _managerApi;
    private readonly _secureChannel;
    private readonly _logger;
    constructor(transportService: TransportService, sessionService: DeviceSessionService, loggerFactory: (tag: string) => LoggerPublisherService, managerApi: ManagerApiService, secureChannel: SecureChannelService);
    private handleDeviceDisconnect;
    execute({ device, sessionRefresherOptions, }: ConnectUseCaseArgs): Promise<DeviceSessionId>;
}
//# sourceMappingURL=ConnectUseCase.d.ts.map