import { Observable } from "rxjs";
import { LoggerPublisherService } from "../../../api/logger-publisher/service/LoggerPublisherService";
import { ConnectedDevice } from "../../../api/transport/model/ConnectedDevice";
import type { DeviceSessionService } from "../../device-session/service/DeviceSessionService";
/**
 * Listen to connected devices
 */
export declare class ListenToConnectedDeviceUseCase {
    private readonly _sessionService;
    private readonly _logger;
    constructor(_sessionService: DeviceSessionService, loggerFactory: (tag: string) => LoggerPublisherService);
    execute(): Observable<ConnectedDevice>;
}
//# sourceMappingURL=ListenToConnectedDeviceUseCase.d.ts.map