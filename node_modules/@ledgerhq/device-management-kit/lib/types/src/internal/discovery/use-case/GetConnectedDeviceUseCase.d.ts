import { DeviceSessionId } from "../../../api/device-session/types";
import { LoggerPublisherService } from "../../../api/logger-publisher/service/LoggerPublisherService";
import { ConnectedDevice } from "../../../api/transport/model/ConnectedDevice";
import type { DeviceSessionService } from "../../device-session/service/DeviceSessionService";
export type GetConnectedDeviceUseCaseArgs = {
    sessionId: DeviceSessionId;
};
/**
 * Get a connected device from deviceSession id.
 */
export declare class GetConnectedDeviceUseCase {
    private readonly _sessionService;
    private readonly _logger;
    constructor(sessionService: DeviceSessionService, loggerFactory: (tag: string) => LoggerPublisherService);
    execute({ sessionId }: GetConnectedDeviceUseCaseArgs): ConnectedDevice;
}
//# sourceMappingURL=GetConnectedDeviceUseCase.d.ts.map