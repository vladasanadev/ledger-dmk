import { LoggerPublisherService } from "../../../api/types";
import { type TransportService } from "../../transport/service/TransportService";
/**
 * Stops discovering devices connected.
 */
export declare class StopDiscoveringUseCase {
    private transportService;
    private readonly _logger;
    constructor(transportService: TransportService, loggerFactory: (tag: string) => LoggerPublisherService);
    execute(): Promise<void>;
}
//# sourceMappingURL=StopDiscoveringUseCase.d.ts.map