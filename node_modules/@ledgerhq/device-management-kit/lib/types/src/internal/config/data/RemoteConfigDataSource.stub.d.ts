import { Either } from "purify-ts";
import { Config } from "../../config/model/Config";
import { RemoteConfigDataSource } from "./ConfigDataSource";
/**
 * class RemoteRestConfigDataSource
 * This is a remote data source that reads the config from a remote API (example).
 */
export declare class StubRemoteConfigDataSource implements RemoteConfigDataSource {
    getConfig(): Promise<Either<never, Config>>;
}
//# sourceMappingURL=RemoteConfigDataSource.stub.d.ts.map