import { Either } from "purify-ts";
import { Config } from "../../config/model/Config";
import { LocalConfigFailure } from "../../config/model/Errors";
import { LocalConfigDataSource } from "./ConfigDataSource";
export declare const stubFsReadFile: () => string;
export declare class FileLocalConfigDataSource implements LocalConfigDataSource {
    /**
     * Retrieves the local configuration.
     * @returns An `Either` containing either a `LocalConfigFailure` or a `Config` object.
     */
    getConfig(): Either<LocalConfigFailure, Config>;
}
//# sourceMappingURL=LocalConfigDataSource.d.ts.map