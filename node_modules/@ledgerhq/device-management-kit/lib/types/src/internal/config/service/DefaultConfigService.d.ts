import type { LoggerPublisherService } from "../../../api/logger-publisher/service/LoggerPublisherService";
import type { LocalConfigDataSource, RemoteConfigDataSource } from "../../config/data/ConfigDataSource";
import { Config } from "../../config/model/Config";
import { ConfigService } from "./ConfigService";
export declare class DefaultConfigService implements ConfigService {
    private _local;
    private _remote;
    private _logger;
    constructor(local: LocalConfigDataSource, remote: RemoteConfigDataSource, loggerServiceFactory: (tag: string) => LoggerPublisherService);
    getDmkConfig(): Promise<Config>;
}
//# sourceMappingURL=DefaultConfigService.d.ts.map