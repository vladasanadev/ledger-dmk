import { Either } from "purify-ts";
import { Config } from "../../config/model/Config";
import { RemoteConfigFailure } from "../../config/model/Errors";
import { RemoteConfigDataSource } from "./ConfigDataSource";
/**
 * class RemoteRestConfigDataSource
 * This is a remote data source that reads the config from a remote API (example).
 */
export declare class RestRemoteConfigDataSource implements RemoteConfigDataSource {
    getConfig(): Promise<Either<RemoteConfigFailure, Config>>;
    private _parseResponse;
    private _callApi;
}
//# sourceMappingURL=RemoteConfigDataSource.d.ts.map