import { type GetAppAndVersionResponse, type TransportConnectedDevice } from "../../../api/index";
import { type Command, type CommandResult, type LoggerPublisherService } from "../../../api/types";
import { type DeviceSessionEventDispatcher } from "../../device-session/model/DeviceSessionEventDispatcher";
type SendCommandFunction = <Response, Args, ErrorStatusCodes>(command: Command<Response, Args, ErrorStatusCodes>, abortTimeout?: number) => Promise<CommandResult<Response, ErrorStatusCodes>>;
export declare class DevicePinger {
    private connectedDevice;
    private _sessionEventDispatcher;
    private readonly _sendCommandFunction;
    private _subscription;
    private _logger;
    constructor(loggerModuleFactory: (tag: string) => LoggerPublisherService, connectedDevice: TransportConnectedDevice, _sessionEventDispatcher: DeviceSessionEventDispatcher, sendCommandFunction: SendCommandFunction);
    ping(): Promise<CommandResult<GetAppAndVersionResponse> | null>;
    private mapEventAction;
    private mapDevicePingAction;
    unsubscribe(): void;
}
export {};
//# sourceMappingURL=DevicePinger.d.ts.map