import { Maybe } from "purify-ts";
import type { ApduSenderService, ApduSenderServiceConstructorArgs } from "../../../api/device-session/service/ApduSenderService";
import { LoggerPublisherService } from "../../../api/logger-publisher/service/LoggerPublisherService";
import { Frame } from "../../device-session/model/Frame";
/**
 * Default implementation of ApduSenderService
 *
 * Split APDU in an array of frames readies to send to a TransportConnectedDevice
 */
export declare class DefaultApduSenderService implements ApduSenderService {
    protected _frameSize: number;
    protected _channel: Maybe<Uint8Array>;
    protected _padding: boolean;
    private _logger;
    /**
     * Constructor
     *
     * @param frameSize
     * @param channel
     * @param padding
     * @param loggerServiceFactory
     */
    constructor({ frameSize, channel, padding, }: ApduSenderServiceConstructorArgs, loggerServiceFactory: (tag: string) => LoggerPublisherService);
    /**
     * Get frames from apdu
     *
     * @param apdu
     */
    getFrames(apdu: Uint8Array): Frame[];
    /**
     * Get apdu frame at index
     * Split every {{PACKET_SIZE - HEADER_SIZE}} bytes of apdu
     * @param apdu
     * @param frameIndex
     * @private
     */
    private getFrameAtIndex;
    /**
     * Get frame header
     * @param frameIndex
     * @param apduSize
     * @private
     */
    private getFrameHeaderFrom;
    /**
     * Get frame offset
     * First frame has more bytes of header
     * Padding append means a 0 bytes is added at the end of each frame
     * @private
     * @param frameIndex
     */
    private getHeaderSizeSumFrom;
    /**
     * Get frame header size
     * @private
     * @param frameIndex
     */
    private getFrameHeaderSizeFromIndex;
}
//# sourceMappingURL=DefaultApduSenderService.d.ts.map