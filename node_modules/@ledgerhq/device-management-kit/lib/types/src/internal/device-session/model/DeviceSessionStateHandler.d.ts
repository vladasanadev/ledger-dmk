import { type BehaviorSubject } from "rxjs";
import { type DeviceSessionState } from "../../../api/device-session/DeviceSessionState";
import { type TransportConnectedDevice } from "../../../api/index";
import { type LoggerPublisherService } from "../../../api/types";
import { type DeviceSessionEventDispatcher } from "../../device-session/model/DeviceSessionEventDispatcher";
export type SetDeviceSessionStateFn = (state: DeviceSessionState) => void;
export declare class DeviceSessionStateHandler {
    private _sessionEventDispatcher;
    private _connectedDevice;
    private _deviceState;
    private setDeviceSessionState;
    private _subscription;
    private readonly _logger;
    constructor(loggerModuleFactory: (tag: string) => LoggerPublisherService, _sessionEventDispatcher: DeviceSessionEventDispatcher, _connectedDevice: TransportConnectedDevice, _deviceState: BehaviorSubject<DeviceSessionState>, setDeviceSessionState: SetDeviceSessionStateFn);
    private _updateDeviceState;
    private mapEventAction;
    private _parseDeviceStatus;
    unsubscribe(): void;
}
//# sourceMappingURL=DeviceSessionStateHandler.d.ts.map