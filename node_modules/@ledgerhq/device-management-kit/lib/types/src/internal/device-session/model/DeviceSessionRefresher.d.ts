import { type TransportConnectedDevice } from "../../../api/transport/model/TransportConnectedDevice";
import { type LoggerPublisherService } from "../../../api/types";
import { type DeviceSessionEventDispatcher } from "../../device-session/model/DeviceSessionEventDispatcher";
export interface DeviceSessionRefresherOptions {
    isRefresherDisabled: boolean;
    pollingInterval?: number;
}
export declare class DeviceSessionRefresher {
    private _sessionEventDispatcher;
    private _refresherSubscription;
    private _refresherOptions;
    private readonly _logger;
    private _connectedDeviceID;
    constructor(loggerModuleFactory: (tag: string) => LoggerPublisherService, refresherOptions: DeviceSessionRefresherOptions, _sessionEventDispatcher: DeviceSessionEventDispatcher, connectedDevice: TransportConnectedDevice);
    startRefresher(): void;
    stopRefresher(): void;
    restartRefresher(): void;
    destroy(): void;
    private getValidPollingInterval;
}
//# sourceMappingURL=DeviceSessionRefresher.d.ts.map