import { type LoggerPublisherService } from "../../../api/types";
export interface RefresherController {
    start(): void;
    stop(): void;
}
export declare class RefresherService {
    private readonly _refresher;
    private readonly _refresherBlockers;
    private readonly _logger;
    constructor(loggerModuleFactory: (tag: string) => LoggerPublisherService, _refresher: RefresherController);
    disableRefresher(id: string): () => void;
    private addRefresherBlocker;
    private removeRefresherBlocker;
}
//# sourceMappingURL=RefresherService.d.ts.map