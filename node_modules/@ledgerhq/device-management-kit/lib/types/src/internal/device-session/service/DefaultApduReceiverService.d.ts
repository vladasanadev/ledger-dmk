import { Either, Maybe } from "purify-ts";
import { ApduResponse } from "../../../api/device-session/ApduResponse";
import { type ApduReceiverConstructorArgs, ApduReceiverService } from "../../../api/device-session/service/ApduReceiverService";
import { LoggerPublisherService } from "../../../api/logger-publisher/service/LoggerPublisherService";
import { ReceiverApduError } from "../../device-session/model/Errors";
export declare class DefaultApduReceiverService implements ApduReceiverService {
    private readonly _channel;
    private readonly _logger;
    private _pendingFrames;
    constructor({ channel }: ApduReceiverConstructorArgs, loggerModuleFactory: (tag: string) => LoggerPublisherService);
    handleFrame(frameBytes: Uint8Array): Either<ReceiverApduError, Maybe<ApduResponse>>;
    private getCompleteFrame;
    private getFrameFromBytes;
    private isComplete;
    private concatFrames;
}
//# sourceMappingURL=DefaultApduReceiverService.d.ts.map